---
phase: 05-progress-wiring-and-ui-polish
plan: 05
type: execute
wave: 4
depends_on: [05-01, 05-02, 05-03, 05-04]
files_modified: []
autonomous: false
requirements: [PROG-02, PROG-03, UIPX-01, UIPX-02, UIPX-03]

must_haves:
  truths:
    - "During a conversion, the UI shows an animated progress indicator that stays active until the SSE stream confirms done"
    - "Uploading a large file shows byte-level upload progress that advances in real time"
    - "The app loads and displays fonts correctly with no internet connection"
    - "A user converting multiple files in bulk can select a different format per file, each row shows individual status"
    - "The dark theme looks visually consistent and polished with emerald accent"
  artifacts: []
  key_links: []
---

<objective>
Verify all Phase 5 features in the browser: progress wiring (single + bulk), upload progress, self-hosted fonts, emerald theme, and bulk workflow improvements.

Purpose: Human confirmation that all five requirements (PROG-02, PROG-03, UIPX-01, UIPX-02, UIPX-03) work correctly in the running application.
Output: Verification pass/fail with issues list if any.
</objective>

<execution_context>
@/home/semal/.claude/get-shit-done/workflows/execute-plan.md
@/home/semal/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-progress-wiring-and-ui-polish/05-CONTEXT.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Full browser verification of Phase 5</name>
  <files>frontend/index.html</files>
  <action>
Start the app with `docker compose up --build -d` (rebuild needed for nginx.conf change and new font files). Open browser to the app URL.

What was built:
- Self-hosted Inter + JetBrains Mono fonts (UIPX-01)
- Emerald dark theme redesign (UIPX-02)
- Single-file two-phase progress bar with SSE (PROG-02, PROG-03)
- Bulk per-row progress, smart defaults, remove button, Download All (UIPX-03)

Verify the following 33 items:

**UIPX-01: Self-hosted fonts**
1. Open the app in browser
2. Open DevTools Network tab, filter by "font"
3. Verify: font files load from `/fonts/inter-*.woff2` and `/fonts/jetbrains-mono-*.woff2` (local paths)
4. Verify: NO requests to `fonts.googleapis.com` or `fonts.gstatic.com`
5. Text should render in Inter (body) and JetBrains Mono (file names, format labels, monospace elements)

**UIPX-02: Emerald dark theme**
6. Background should be near-black (#0a0a0a)
7. Accent color should be emerald/green (not cyan)
8. Sidebar logo, active nav item, format chips, buttons should all use emerald
9. Cards should have subtle borders and shadows
10. Hover states should animate smoothly (buttons, chips, nav items)
11. Status badges (document, image, audio, etc.) should have distinct colors

**PROG-02 + PROG-03: Single-file progress**
12. Upload a single file (ideally a video or large image for visible progress)
13. Watch the progress bar during upload: should advance from 0% with "Uploading..." label
14. After upload, select a format and click Convert
15. Watch progress bar during conversion: should advance from ~50% with "Converting..." label
16. On completion: bar at 100%, Download button appears below
17. Click Download — file should download correctly
18. Try an invalid conversion (if possible): error message should appear with details toggle

**UIPX-03: Bulk workflow**
19. Upload 3+ files at once (drag or select)
20. Verify: each row shows a smart-default format pre-selected
21. Verify: each row has an X button to remove it
22. Click X on one row — it should be removed from the list
23. Change format on individual rows using the per-row dropdown
24. Click Convert All
25. Watch: each row's mini progress bar should animate independently
26. After completion: summary banner shows "X of Y converted"
27. Verify: each successful row has a Download button (down-arrow)
28. Click individual download — file should download
29. Click "Download All as ZIP" button — a single ZIP file should download containing all converted files
30. Verify: failed rows show error icon and error message

**Cross-cutting**
31. Navigate to History page — recent conversions should appear
32. Navigate to About page — should use new theme colors
33. Responsive: resize browser to mobile width — sidebar should collapse to horizontal nav

Resume signal: Type "approved" to complete Phase 5, or describe any issues found.
  </action>
  <verify>
    <automated>curl -s http://localhost:8071/ | grep -q "app.css" && echo "PASS" || echo "FAIL"</automated>
    <manual>All 33 verification items pass in browser.</manual>
  </verify>
  <done>Human approves all Phase 5 features: fonts load locally, emerald theme is polished, single-file progress works end-to-end, bulk workflow has per-row tracking with smart defaults and Download All.</done>
</task>

</tasks>

<verification>
All 33 verification items pass in browser.
</verification>

<success_criteria>
- Human approves that all PROG-02, PROG-03, UIPX-01, UIPX-02, UIPX-03 features work correctly
- No visual regressions from Phase 4
- Progress bars animate smoothly, no jumps from 0% to 100%
- Fonts load locally with no CDN requests
- Theme is visually polished and consistent
</success_criteria>

<output>
After completion, create `.planning/phases/05-progress-wiring-and-ui-polish/05-05-SUMMARY.md`
</output>
